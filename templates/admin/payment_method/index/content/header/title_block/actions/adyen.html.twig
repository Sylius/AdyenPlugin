{% import '@SyliusAdmin/shared/helper/dropdown.html.twig' as dropdown %}

{{ dropdown.list(
    {
        direction: 'down-end',
        button: { text: 'sylius_adyen.ui.adyen_gateway_label'|trans, icon: 'tabler:credit-card' }
    },
    hookable_metadata.configuration.items|map(item => {
        title: item.title|trans,
        url: item.path is defined ? path(item.path, item.parameters|default({})) : null
    })|filter(item => item.url is not null)
) }}
