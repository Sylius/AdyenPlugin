sylius_adyen:
    api_key:
        not_blank: API key cannot be blank.
    auth_password:
        not_blank: Notification password cannot be blank.
    auth_user:
        not_blank: Notification user cannot be blank.
    credentials:
        invalid_api_key: Provided API key is invalid.
        invalid_merchant_account: Provided Merchant Account name is invalid (it's case-sensitive).
    environment:
        not_blank: Environment cannot be blank.
    hmac_key:
        not_blank: HMAC Key cannot be blank.
    merchant_account:
        not_blank: Merchant account cannot be blank.
